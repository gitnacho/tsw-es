//// { "compiler": {  }, "order": 1 }

// TypeScript admite muchas correcciones, que están automatizadas
// por ayudantes para refactorizar tu código. Por ejemplo, si seleccionas
// el texto de la línea 7 y haces clic en la bombilla que
// aparece, se te ofrecerán algunas correcciones.

function addOne(x: number) {
  return x + 1;
}

// Esta característica está disponible a partir de la versión 3.7 de TypeScript,
// que también incluirá las construcciones nocturnas.

// Puede que no sean algo que necesites usar dentro del
// playground cuando estás haciendo muestras de código o aprendiendo.

// Sin embargo, tener correcciones disponibles significa que podemos documentar
// en el playground y eso es realmente valioso:

// example:big-number-literals
// example:const-to-let
// example:infer-from-usage-changes
